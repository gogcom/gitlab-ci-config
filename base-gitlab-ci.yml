build:
    script:
      - npm install
      - npm run build
      - npm run test
    except:
      - master

publish:
    script:
      - npm install
      - echo '//npm-registry.gog.prod:4873/:_authToken="$AUTH_TOKEN"'>.npmrc
      - npm run publish-please --ci
    only:
      - master
